version: "3.8"

services:
  db:
    image: postgres:14-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ProNet
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      JWT_SECRET: quickbrownfox
      DB_CLIENT: pg
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: ProNet
      CLOUDINARY_CLOUD_NAME: saradchhetri
      CLOUDINARY_API_KEY: 142726463693557
      CLOUDINARY_API_SECRET: T8_33FaSXyH89vweqqvrTjfBgbM
    depends_on:
      - db

volumes:
  pgdata:
